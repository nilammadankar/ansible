- name: playbook4
  hosts: all
  tasks:
   when:  ansible_system_capabilities_enforced== "false" and ansible_distribution== "false"
  - name: setup file execution
    debug:
     msg: "ansible_distri: {{ansible_distribution}}"
  - name: setup execution2
    debug:
     msg: "ansible capabilities: {{ansible_system_capabilities_enforced}}"
  - name: ansible shell
    debug:
     msg: " for ansible shell: {{ansible_user_shell}}"
  